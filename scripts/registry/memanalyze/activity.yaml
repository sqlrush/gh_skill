# ⚠️ 本文件由 tools/grmp_gen_memanalyze.py 生成，请勿手工编辑。
#
# memanalyze 的 SQL 正文取决于目标实例有哪些视图、每个视图有哪些列 ——
# 探测到什么就查什么，缺的列补 NULL。固定白名单覆盖不了这种形态，
# 所以脚本名固定、正文按实例生成。
#
# 生成依据（来自实例 og）：
#   视图  pg_stat_activity
#   列    datid, datname, pid, sessionid, usesysid, usename, application_name, client_addr, client_hostname, client_port, backend_start, xact_start, query_start, state_change, waiting, enqueue, state, resource_pool, query_id, query, connection_info, unique_sql_id, trace_id
#
# ⚠️ 换一套实例必须重新生成。

name: memanalyze.activity
description: 会话在跑什么（用于关联）
sql: |
  SELECT sessionid, pid, usename, application_name, state, query FROM pg_stat_activity;
