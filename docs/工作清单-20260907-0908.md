# 工作清单：2026-09-07 ～ 09-08

两天内合入 main 的工作，按先后排列。括号里是对应的标签或提交。

1. gaussdb-kb 加文件模式：没有向量库和图库也能导入、检索知识库（skills-v9）。
2. 直连驱动 pg8000 整体换成 psycopg2 2.9.10，不留别名，离线安装件备好（bba401b）。
3. 分析客户 8 张生产报错照片，四类报错逐条归因，写反馈稿。
4. explain / sqltune / vacuum 运行前探测 gs_get_explain、gs_get_kernel_info，有则用运行态计划和内核水位，无则退回（skills-v10）。
5. 备机、权限不足、函数不存在三类报错翻成中文说明并给核对步骤。
6. 按客户 pg_catalog 截图把 gs_get_explain 改成实测的 bigint 签名，integer 版本作备用脚本（skills-v10.4）。
7. 交付验收四层测试（解包安装、直连加中间件两条路、桩函数模拟 GaussDB、模型级），抓出并修复三个真缺陷（skills-v10.1、v10.2）。
8. ANALYZE 固定关闭的三条脚本改成估算口径，skill 按计划文本判断有没有真 analyze（skills-v10.3）。
9. 会话按句柄分文件，17 个 skill 收 --session，多会话无句柄拒绝，修同沙箱多用户串库（skills-v11）。
10. EXPLAIN 前先切 search_path 的两语句模板加探测保险，修 sqltune 表名不带 schema 的 400（skills-v12）。
11. procinfo / proctune 支持包内过程三段名，同名过程不猜，修 A 兼容库空串条件（skills-v12.1）。
12. sqltune 代价推演失败降级不崩，中间件路径的写语句在 skill 层拦下（skills-v12.2）。
13. 目录证据按 schema 过滤，同名表跨 schema 不再混行，占位符按本 schema 列类型合成（skills-v12.3）。
14. search_path 没切成时给可照做的 DBA 命令，并分清白名单没灌和执行器不支持两种原因（skills-v12.4）。
15. 白名单从 82 条增到 96 条，交付文档、排障表、SKILL.md 同步，每轮都重打交付包并装到正式目录。
16. 对照客户 09-08 问题清单逐项核对，三份反馈稿写好（存本地 prd，不入库）。
17. 过程找不到时脚本代为排查是连错库、执行账号无权限（对象隔离藏 schema）还是本身没有，给用户编号问题清单与 DBA 核对 SQL；知识库导入遇本机路径时指明沙箱收件目录（skills-v12.5，09-09）。
18. search_path 两语句模板在客户执行器上跑不了时，脚本自己把不带 schema 的表名补全后取计划；explain 加 `--sql-id` 自动带 statement_history 的 schema；SKILL.md 禁止猜 schema、首选按 id（skills-v12.6，09-09）。

当前基线：main 上 skills-v12.6；单测 2122 通过、46 跳过。
