# kb skill P1 实施清单

> 设计:`docs/superpowers/specs/2026-09-02-knowledge-base-skill-design.md`(v3)。分支 `feat/kb`。
> 进度以本文件的勾选为准(跨会话续跑)。每个任务:先写测试,再写实现,单测绿 + 该任务的真跑验证才勾。
> Python 只在 Mac 上跑:`ssh sqlrush@192.168.128.1 'cd /Users/sqlrush/opencode_skill && …'`。

## 环境

- [ ] E1 Mac:pg8000 / cryptography / PyYAML / pytest 齐;numpy **不装**(先验纯 Python 路径,P2 再验加速路径)
- [ ] E2 Docker:`opengauss/opengauss-server:7.0.0-RC2.B015` → og7 :5439;`pgvector/pgvector:pg16` → kbpg :5440;`neo4j:5-community` → kbneo4j :7474/7687
- [ ] E3 凭据:`~/.gdaa/credentials/` 里 `kb-og7` `kb-pg` `kb-graph` 三个加密口令(经 credential_cli,不落明文)

## 代码

- [x] T1 kbimport → kb 改名替换:目录、SKILL.md(name/version 2.0.0)、`kb.py`、references、契约标记文案、tests、install 脚本与交付文档里的引用;旧单测全绿
- [x] T2 `common/kb/config.py`:读 `<kb>/kb.yaml`、OpenCode provider 配置(base URL + key 环境变量名,不打印)、凭据名;`text.py`:归一化、二元组 + 标识符分词、tsquery 组装
- [x] T3 `common/kb/store_pg.py`:探测 vector/hnsw、建表、upsert docs/chunks/node_vectors、向量/词法查询、rebuild;live 测试 pgvector ✅ + og7(openGauss 7.0.0-RC1 DataVec)✅ ——同一套 SQL 两种引擎
- [x] T4 `common/kb/store_graph.py`:Neo4j HTTP Query API 客户端、约束、MERGE 节点/边、有界路径查询、rebuild、不可达降级;live 测试打 neo4j ✅
- [x] T5 `common/kb/embed.py`:OpenAI 兼容 embeddings 客户端、内容哈希缓存、逐块失败隔离、超时;单测用本地假 HTTP 服务 ✅
- [x] T6 案例格式:`references/case-format.md`、`cases/*.md` 解析与 validate(frontmatter + 四节 + 出处回指)、`graph/*.yaml` + `canonical.yaml` 解析与 validate
- [x] T7 `common/kb/indexer.py`(`kb.py index` 的 CLI 接线在 T9/T10 一起做):文件 → 切块 → 词法 → 向量 → PG;节点/边 → Neo4j;节点向量;`index/state.json` 水位;覆盖率门(退出码 2);`--rebuild`
- [ ] T8 `common/kb/query.py` + `render.py`:from_findings / from_text、RRF、分类型阈值、路径组装、「客户知识库参照」小节、状态行、misses.log
- [x] T9 `kb.py ingest`(csv/xlsx、一单一文件、raw 入库、--redact)、`propose`(工作单)、`review`(校验 + 编号选择列表)、`apply`(decisions → 文件)
- [x] T10 `kb.py setup / health / eval / feedback`
- [x] T11 `skills/kb/SKILL.md` + references(selection-list.md、graph-schema.md、storage-setup.md)+ `kb-contract.md` 升级 + `contract --apply` 到 9 个 skill
- [x] T12 health 与 sqltune 脚本层接入(固定小节);渲染断言测试
- [x] T13 语料:24 案例 + 1 规范(12 条款)+ 3 干扰,5 金丝雀;`eval/queries.yaml`
- [ ] T14 三态对照真跑(og5 跑 skill,og7/pgvector + neo4j 做知识库):导入前 / 导入后 / 删库回退,结果记 `scripts/kb-e2e.md`
- [ ] T15 交付文档、CHANGELOG、记忆;合并策略与 gh_skill 同步方案

## 记录

(每完成一项在此追加一行:日期 · 任务 · 提交 · 真跑结果)
- 2026-09-02 · T1–T10 · gh_skill feat/kb(08765ce 起)· pgvector+og7+neo4j live 全过;真容器冒烟(~/kb-smoke.sh):ingest→propose→review→apply→index→query 命中案例与路径,无关问题全「无」
