"""GRMP 协议的**运行时共用部分**。

这里放的是两条数据库访问路径都要用到的东西：脚本定义模型、占位符渲染器、
结果序列化、兼容性开关。

为什么在 common/ 而不在 tools/：渲染器必须由中间件路径与直连路径共用。
两侧各有一份的话，同一模板在两侧会渲染出不同的 SQL，双路径一致性测试
比对的就成了两条不同的 SQL，而不是两条不同的执行链路 —— 测试本身失去意义。

grmp_middleware/grmp_mock/ 里剩下的是开发期工具（HTTP 服务、注册工具、DML 导出、
风险标注、SQLite 存储），skill 运行时不依赖它们。
"""
