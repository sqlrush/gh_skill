"""（已清空）skill 的 SQL 定义。

原先这里放着 49 条 skill_* SQL 模板。它们已全部迁到

    scripts/registry/<skill>/<脚本名>.yaml

那里是 SQL 的**唯一来源**：中间件路径与直连路径共用同一份定义。

为什么不留一份副本在这里 —— 那就是两个事实源。改了一处忘了另一处，
两条路径跑的会是不同的 SQL，而双路径一致性测试恰恰废在这种情况上：
它比的是「两条执行链路」，不是「两份 SQL」，两边各错各的它照样是绿的。

迁移时顺带修掉/记录的模板缺陷见 docs/test/ 下的迁移报告。

本文件保留（不删）只为兼容可能存在的外部 import；新增 SQL 请写 YAML。
"""
